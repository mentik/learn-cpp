#include <iostream>
using namespace std;

// Namespace std is used to avoid prefixing standard library names with "std::", making the code cleaner for simple programs.
int main() {
    cout << "Hello, World! Using Namespace std!" << endl;
    return 0;
}
